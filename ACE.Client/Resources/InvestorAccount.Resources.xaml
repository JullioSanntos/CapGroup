<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr -namespace:ACE.Client.Resources" >
   <DataTemplate x:Key="InvestorAccount.Detail.Column" >
      <StackPanel Orientation="Vertical" >
         <StackPanel Orientation="Horizontal" Margin="4, 2" >
            <TextBlock Text="Number:" Width="78" FontWeight="DemiBold" Foreground="Blue" />
            <TextBox Text="{Binding EditingEntity.Number}" Width="80" />
         </StackPanel>
         <StackPanel Orientation="Horizontal" Margin="4, 2" >
            <TextBlock Text="Curr Acct Val:" Width="78" FontWeight="DemiBold" Foreground="Blue" />
            <TextBox Text="{Binding EditingEntity.CurrAcctVal}" Width="80" />
         </StackPanel>
         <StackPanel Orientation="Horizontal" Margin="4, 2" >
            <TextBlock Text="Full Name:" Width="78" FontWeight="DemiBold" Foreground="Blue" />
            <TextBox Text="{Binding EditingEntity.FullName, Mode=OneWay}" Width="100" IsReadOnly="True" />
         </StackPanel>
      </StackPanel>
   </DataTemplate>
</ResourceDictionary>